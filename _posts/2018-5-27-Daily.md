---

layout:     post   				    # 使用的布局（不需要改）
title:      无题 				# 标题 
subtitle:   雪的世界 #副标题
date:       2018-05-27			# 时间
author:     LYC					# 作者
header-img: img/post-bg-2015.jpg 	#这篇文章标题背景图片
catalog: true 						# 是否归档
tags:  生活

---

做了一个好梦，梦到和表弟他们打雪仗，好像很快乐的样子。

很难得。好像很久都是做一些荒诞不堪的梦，完全搞不懂意思；或者是一些噩梦；又偶尔
午觉的时候受到梦魇的折磨。

昨天在调一道 **提高+/省选-** 的搜索题，爆零。
爆零代码如下：

```
//每一个不曾AC的日子都是一种遗憾
#include<stdio.h>
#include<set>
#include<string>
#include<sstream>
#define MAXN 10000000
#define TARGET 123804765
using namespace std;

bool ok=false;
int q[MAXN][2];
int front,rear;
set<int> a;
const int b[9][5]={1,3,-1,-1,-1
                  ,0,2,4,-1,-1
                  ,1,5,-1,-1,-1
                  ,0,4,6,-1,-1
                  ,3,5,1,7,-1
                  ,2,4,8,-1,-1
                  ,3,7,-1,-1,-1
                  ,6,4,8,-1,-1
                  ,7,5,-1,-1,-1};

inline void _swap(char &ch1,char &ch2)
{
    char ch3=ch1;
    ch1=ch2;
    ch2=ch3;
}

inline string numToStr(int i)  
{  
    stringstream ss;  
    ss<<i;
    return ss.str();  
}  

inline int strToNum(string s)  
{  
    int num;  
    stringstream ss(s);  
    ss>>num;
    return num;  
}

inline int find_0(int x)
{
    string st=numToStr(x);
    if(st.size()==8)return 0;
    for(int i=0;st[i]!='\0';i++)
    {
        if(st[i]=='0')
        return i;
    }
}

inline int transf(int n,int s,int e)
{
    string st=numToStr(n);
    _swap(st[s],st[e]);
    return strToNum(st);
}

inline void BFS()
{
    while(front<=rear)
    {
        int qwq=q[front][0];
        int pos=find_0(qwq);
        for(int i=0;b[pos][i]!=-1;i++)
        {
            int nqwq=transf(qwq,pos,b[pos][i]);
            if(a.count(nqwq))continue;
            else{
                if(nqwq==TARGET){
                    printf("%d",q[front][1]+1);
                    ok=true;
                    return;
                }
                q[rear][0]=nqwq;
                q[rear++][1]=q[front][1]+1;
                a.insert(nqwq);
            }
        }		        front++;
    }
}

int main()
{
    //freopen(".in","r",stdin);
    //freopen(".out","w",stdout);
    int n;
    scanf("%d",&n);
    q[0][0]=n; q[0][1]=0;
    rear++;
    a.insert(n);
    
    BFS();

    if(ok)return 0;
    printf("no solution");
    return 0;
}
```
> END
